spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:5001
      fail-fast: true
  profiles:
    active: dev

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  security:
    enabled: false